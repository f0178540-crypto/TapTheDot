enum SkillType {
  extraTime,        // + ms per round
  biggerRadius,     // target hit radius multiplier
  bonusTimeBoost,   // TimeDot bonus multiplier
  bombChanceReduce, // lower bomb spawn chance
}
