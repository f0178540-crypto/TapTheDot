name: tap_the_dot
description: Tap The Dot - Arcade Reflex Game
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # STATE
  provider: ^6.1.2

  # STORAGE
  shared_preferences: ^2.2.3
  path_provider: ^2.1.2

  # ADS
  google_mobile_ads: ^5.2.0

  # IAP
  in_app_purchase: ^3.2.0

  # WEBVIEW
  webview_flutter: ^4.8.0

  # SHARE / VIRAL
  share_plus: ^7.2.1

  # FIREBASE (KOMPATIBILNO SA FLUTTER 3.16)
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/
